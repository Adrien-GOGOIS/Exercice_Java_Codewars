package codewars;

public class NumberToString {
    public static String numberToString(int num) {
        String str = Integer.toString(num);
        return str;
    }

    // Ou : return String.valueOf(num);
}
